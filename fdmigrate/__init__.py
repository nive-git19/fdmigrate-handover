"""Standalone Freshdesk -> Freshdesk migration tool.

A reusable, resumable, rate-limit-safe migrator for moving data between any two
Freshdesk accounts. Engine logic ported from the verified Odoo `freshdesk_migrator`
V1 module; runner shape adapted from the standalone `migrate.py` reference.
"""

__version__ = "1.0.0"
